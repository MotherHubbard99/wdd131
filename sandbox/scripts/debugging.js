const pi = 3.14;
//will change so use let instead of const
let area = 0;
//calculate the area of each circle
function circleArea(radius) {
    const area = radius * pi;
    return area
}
area = circleArea(3)
console.log('Area2: area');

